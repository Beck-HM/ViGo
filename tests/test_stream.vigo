# ============================================
#   Stream Output Test — Ollama gemma-4b
# ============================================

print("=== Stream Output Test (gemma-4b) ===")
print("")

print("--- Streaming: 'Count from 1 to 5 slowly.' ---")
result = ai_ask("Count from 1 to 5 slowly.", "gemma-4b", 0.7, 200, "ollama", ok)

chunks = ai_stream_chunks()
print("")
print("--- Complete ---")
print("Chunks: " + str(len(chunks)))
print("Full length: " + str(len(result)))

if len(chunks) > 0 ts
    print("First 5 chunks:")
    i = 0
    loop i < 5 and i < len(chunks) ts
        print("  [" + str(i) + "]: " + chunks[i])
        i = i + 1
    Fin
Fin